# ОнкоАссистент

**ИИ-помощник для проверки планов лечения рака**  
Проверяет соответствие последним рекомендациям
и объясняет план лечения пациенту простыми словами (агент пациента).

---

## 🚀 Быстрый запуск

### 1. Клонируйте репозиторий
```bash
git clone https://github.com/LenyOstrich/ai-onco-hackathon.git
cd ai-onco-hackathon
```

### 2. Создайте файл .env в корне проекта
```
YANDEX_API_KEY=ваш_ключ
YANDEX_FOLDER_ID=ваш_folder_id
YANDEX_DOCTOR_AGENT_ID=id_агента_для_врача
YANDEX_PATIENT_AGENT_ID=id_агента_для_пациента
```

### 3. Установите зависимости
```bash
pip install -r requirements.txt
```

### 4. Запустите сервер
```
uvicorn main:app --reload
```

### 5. Откройте интерфейсы в браузере
Интерфейс врача → http://127.0.0.1:8000/

Интерфейс пациента → http://127.0.0.1:8000/patient.html

### 6. Загрузите файлы в формате txt
Не загружайте очень длинные файлы, так как у инструментов Яндекса есть ограничение на количество токенов в ответе по API и решение может упасть

### Структура проекта
```text
ai-onco-hackathon/
├── main.py                 # FastAPI приложение
├── ai_client.py            # Клиент для Yandex Cloud агентов
├── requirements.txt
├── .gitignore
├── static/
│   ├── index.html          # Интерфейс врача
│   └── patient.html        # Интерфейс пациента
└── README.md
```

### Технологии
* FastAPI + Uvicorn
* Yandex Cloud Agents (два агента: врач + пациент)
* Tailwind CSS + MathJax (для красивого отображения)
* python-dotenv

Кейсы на которых тестировалось решение, а так же параметры настройки агентов в закрытой папке (доступ только по почте организаторов).
